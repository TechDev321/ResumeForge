# Public API base URL (no trailing slash). Set this on Vercel as an env var.
# Localhost does not need this — app.js falls back to http://127.0.0.1:8000.
RESUMEFORGE_API_BASE=https://resumeforge-api-jsaa.onrender.com
